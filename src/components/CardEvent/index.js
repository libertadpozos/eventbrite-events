import React from 'react';

const CardEvent = () => {
  return (
    <div className="card__container">
      {/* <img className="card__image" src={image} alt={description} />
      <p className="card__event-date">{date}</p>
      <h2 className="card__event-name">{name}</h2>
      <p className="card__event-place">{place}</p> */}
    </div>
  );
};

export default CardEvent;
